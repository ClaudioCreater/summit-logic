# -*- coding: utf-8 -*-
"""
data_cleaner.py
───────────────
대한통운 LOIS 업로드 전 데이터 정제 함수 모음.

- clean_text()       : 이름·주소·배송메시지에서 이모지·제어문자 제거
- clean_phone()      : 전화번호 숫자만 추출 (하이픈·공백 제거)
- truncate_address() : 주소 길이 초과 시 잘라냄 (CJ LOIS 업로드 제한 대응)
"""

import re

# CJ LOIS 주소 필드 최대 허용 길이 (문자 수)
ADDRESS_MAX_LEN = 100

# ── 이모지·특수기호 범위 사전 컴파일 (모듈 로드 시 1회만 실행) ──
_EMOJI_RE = re.compile(
    "["
    "\U0001F000-\U0001FFFF"   # Misc Symbols / Emoticons / Transport 등
    "\U00002600-\U000027BF"   # Misc Symbols, Dingbats
    "\U0000200B-\U0000200F"   # Zero-width chars (ZWSP, ZWNJ, ZWJ, LRM, RLM)
    "\U0000FE00-\U0000FE0F"   # Variation Selectors
    "]+",
    flags=re.UNICODE,
)

# NaN·빈값 판별 문자열 목록
_NULL_STRINGS = {"nan", "none", "null", "n/a", "-", ""}


def clean_text(text: str) -> str:
    """
    이름·주소·배송메시지에서 이모지 및 제어문자를 제거합니다.

    보존 대상: 한글, 영문, 숫자, 공백, 기본 구두점(-.,()/ 등)
    제거 대상: 이모지(😊🎉 등), 탭·줄바꿈 등 제어문자, Zero-width 문자

    Example:
        >>> clean_text("홍길동😊\\n서울시")
        '홍길동 서울시'
        >>> clean_text("서울시 🏠 강남구 🚀 테헤란로")
        '서울시 강남구 테헤란로'
    """
    text = _EMOJI_RE.sub("", str(text))
    # 탭·줄바꿈·기타 제어문자를 공백으로 치환
    text = re.sub(r"[\x00-\x1f\x7f]", " ", text)
    # 연속 공백 압축
    text = re.sub(r" {2,}", " ", text)
    return text.strip()


def clean_phone(phone: str) -> str:
    """
    전화번호를 숫자만 남긴 표준 형식으로 변환합니다.

    처리 케이스:
    - 010-1234-5678        → 01012345678  (하이픈 제거)
    - (010) 1234 5678      → 01012345678  (괄호·공백 제거)
    - +82-10-1234-5678     → 01012345678  (국제번호 한국 형식 변환)
    - 82 10 1234 5678      → 01012345678  (국가코드 제거)
    - 010.1234.5678        → 01012345678  (마침표 제거)
    - nan / None / 공백    → ""           (빈값 반환)

    Example:
        >>> clean_phone("+82-10-1234-5678")
        '01012345678'
        >>> clean_phone("nan")
        ''
    """
    text = str(phone).strip()

    # NaN·빈값 조기 반환
    if text.lower() in _NULL_STRINGS:
        return ""

    # 숫자만 추출
    digits = re.sub(r"[^0-9]", "", text)

    if not digits:
        return ""

    # 한국 국제전화 코드 +82 처리
    # 82로 시작하고 11~13자리이면 "82" → "0" 으로 치환
    # 예) 821012345678 (12자리) → 01012345678 (11자리)
    if digits.startswith("82") and 11 <= len(digits) <= 13:
        digits = "0" + digits[2:]

    return digits


def truncate_address(address: str, max_len: int = ADDRESS_MAX_LEN) -> str:
    """
    주소가 CJ LOIS 업로드 길이 제한을 초과하면 잘라냅니다.

    Args:
        address: 원본 주소 문자열
        max_len: 허용 최대 길이 (기본값: ADDRESS_MAX_LEN = 100)

    Example:
        >>> truncate_address("A" * 120)  # 100자로 절삭
        'AAAA...(100자)'
    """
    return address[:max_len] if len(address) > max_len else address
